@model PerfilViewModel
@using RedeSocial.Apresentacao.Controllers;

<div class="container-fluid gedf-wrapper">
    <div class="row">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="h5"></div>
                    <div class="h4"><img class="rounded-circle" src="@Model.FotoPerfil" width="45"/> @Model.NomePerfil</div>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        @if (Model.StatusAmizade == 0)
                        {
                            <form asp-action="Create" asp-route-user="@Model.IdentityUserPerfil" method="post">
                                <button class="btn btn-primary">Solicitar Amizade <i class="fa fa-user-plus"></i></button>
                            </form>

                        }
                        else if (Model.StatusAmizade == 1)
                        {
                            <button class="btn btn-warning" disabled>Amizade Solicitada</button>
                        }
                        else if (Model.StatusAmizade == 2)
                        {
                            <button class="btn btn-primary">Amigos <i class="fa fa-check-circle"></i></button>

                            <form asp-action="Delete" asp-route-user="@Model.IdentityUserPerfil" method="post">
                                <button class="btn btn-danger">Desfazer Amizade <i class="fa fa-user-times"></i></button>
                            </form>

                        }
                                </li>
                    <li class="list-group-item">
                        <div class="h6 text-muted">Amigos</div>
                        <div class="h5">52342</div>
                    </li>

                </ul>
            </div>
        </div>
        <div class="col-md-6 gedf-main">

            <!--- \\\\\\\Post-->
            <div class="card gedf-card">
            </div>
            <!-- Post /////-->
            <!--- \\\\\\\Post-->

            @foreach (var post in Model.ListaPost)
            {
<div class="card gedf-card">
    <div class="card-header">
        <div class="d-flex justify-content-between align-items-center">
            <div class="d-flex justify-content-between align-items-center">
                <div class="mr-2">
                    <img class="rounded-circle" width="45" src="@post.FotoPerfil" alt="">
                </div>
                <div class="ml-2">
                    <div class="h5 m-0"></div>
                    <div class="h7 text-muted">@post.NomeCompleto</div>
                </div>
            </div>
            @if (post.IdentityUser == Model.IdentityUserLogado)
            {
<div>
    <div class="dropdown">
        <button class="btn btn-link dropdown-toggle" type="button" id="gedf-drop1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-ellipsis-h"></i>
        </button>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="gedf-drop1">
            <div class="h6 dropdown-header">Configuration</div>
            <a class="dropdown-item" id="register" asp-controller="Post" asp-action="Delete" asp-route-id="@post.Id">Excluir</a>

        </div>
    </div>
</div>}
        </div>
    </div>
    <div class="card-body" style="border-bottom:1px solid rgba(0,0,0,.125);">
        <div class="text-muted h7 mb-2"> <i class="fa fa-clock-o"></i> @post.TempoDaPostagem @post.FormatacaoTempo atrás</div>
        <p class="card-text">
            @post.Texto
        </p>
    </div>
    @if (post.UriImage != null)
    {
        <div class="card-body">
            <img src="@post.UriImage" width="100%" />
        </div>

    }

    <div class="card-footer">
        <a href="#" class="card-link"><i class="fa fa-gittip"></i> Like</a>
        <a href="#" class="card-link"><i class="fa fa-comment"></i> Comment</a>
        <a href="#" class="card-link"><i class="fa fa-mail-forward"></i> Share</a>
    </div>
</div>            }

            <!-- Post /////-->
            <!-- Post /////-->
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="h5"></div>
                    <div class="h4 text-muted">Lista de amigos</div>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        <div class="h5"><img src="" />  Amigo 1</div>
                    </li>
                    <li class="list-group-item">
                        <div class="h5"><img src="" /> Amigo 2</div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>

